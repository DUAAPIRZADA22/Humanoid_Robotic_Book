# VERCEL PRODUCTION ENVIRONMENT VARIABLES
# Add these in Vercel Dashboard > Settings > Environment Variables

# Backend API URL (Railway)
CHAT_API_ENDPOINT=https://humanoid-robotic-book-backend.up.railway.app

# Translation API URL (same as backend)
TRANSLATE_API_URL=https://humanoid-robotic-book-backend.up.railway.app

# API Key (optional - backend doesn't require it)
CHAT_API_KEY=
